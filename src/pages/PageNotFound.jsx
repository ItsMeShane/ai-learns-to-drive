import React from "react";
import { Wrapper } from "../GlobalComponents";

const PageNotFound = () => {
    return (
        <Wrapper>
            page not found
        </Wrapper>
    );
};

export default PageNotFound;